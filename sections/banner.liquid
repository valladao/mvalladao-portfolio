{% render 'banner', heading: section.settings.heading, subheading: section.settings.subheading %}
{% schema %}
{
  "name": "Banner",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading"
    },
  ],
  "presets": [
    {
      "name": "Banner"
    }
  ]
}
{% endschema %}
